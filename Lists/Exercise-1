#turning Pascal and Camelcase to snakecase using list comprehension
def pascal_to_snake_case(string):
    string_list = ['_'+char.upper() if char.isupper() else char for char in string]
    new_string = ''.join(string_list).strip()
    return new_string

variable = 'PascalCase'
pascal_to_snake_case(variable)